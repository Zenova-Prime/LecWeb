// src/components/Introduction.js
import React from "react";

const Introduction = () => (
  <section className="text-center mb-8">
    <p
      className="text-lg intro-text"
      style={{
        fontFamily: "Montserrat, sans-serif",
        maxWidth: "1000px",
        overflowWrap: "break-word",
      }}
    >
      We provide easy access to lectures that are already available on various
      educational platforms and Telegram channels, curated for your convenience.
    </p>
  </section>
);

export default Introduction;
